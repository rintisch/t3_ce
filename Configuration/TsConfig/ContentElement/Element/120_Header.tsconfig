#
# TCEFORM FOR CONTENT ELEMENT: Header
# This is not integrated in content element `textpic`
# because the image crop variants are very different
# for this content element.
#

TCEFORM.tt_content.layout.types {
  header {
    addItems {
      10 = Mit Bild
    }
  }
}
